export default {
    MUTATION_FORM_TREE_DATA(state, visable) {
        state.mutationFormTreeData = visable;
    },
    MUTATION_V_LOADING(state, visable) {
        state.VLoading.loading = visable;
    },
}